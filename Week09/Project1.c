#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int flag = 1;
	
	while(flag!=0){
		int y;
		y = 3;
		flag = 0;
		
		y=4;
	}
	
	//y=4; //변수 y는 while {} 내에서만 존재하는 지역 변수이므로 while 밖에서 존재하지 않는다.  
	
	return 0;
}
